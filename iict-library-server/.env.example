# Runtime
NODE_ENV=development
PORT=5000

# CORS
CORS_ORIGIN=http://localhost:5173

# MariaDB + Prisma (provider: mysql)
DATABASE_URL="mysql://root:password@localhost:3306/iict_library"

# Note
# Current auth middleware is a development bridge that reads request headers:
# x-user-id and x-user-role (ADMIN|STUDENT|TEACHER).
